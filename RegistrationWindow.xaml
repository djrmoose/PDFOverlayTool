<Window x:Class="PdfOverlayTool.RegistrationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Welcome"
        Icon="/Resources/OCT.ico"
        Width="540"
        Height="Auto"
        SizeToContent="Height"
        MinWidth="480"
        MaxHeight="720"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F0F0F0">

        <Window.Resources>
                <SolidColorBrush x:Key="AccentBrush" Color="#7A5230"/>
                <SolidColorBrush x:Key="TextBrush" Color="#3A2E24"/>
                <SolidColorBrush x:Key="BodyBrush" Color="#4A3F35"/>
        </Window.Resources>

        <Grid Margin="24">
                <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="TitleTextBlock"
                           Grid.Row="0"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource AccentBrush}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,8"/>

                <TextBlock Grid.Row="1"
                           FontSize="13"
                           Foreground="{StaticResource BodyBrush}"
                           TextWrapping="Wrap"
                           LineHeight="20"
                           Margin="0,0,0,12"
                           Text="Please enter your name and email for beta support. Anonymous usage data (settings, session activity, and file statistics without file names) is sent automatically as described in the terms below."/>

                <TextBlock Grid.Row="2"
                           Text="{x:Static local:BetaTerms.Title}"
                           xmlns:local="clr-namespace:PdfOverlayTool"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextBrush}"
                           Margin="0,0,0,6"/>

                <Border Grid.Row="3"
                        BorderBrush="#CCCCCC"
                        BorderThickness="1"
                        Background="White"
                        Margin="0,0,0,12"
                        MaxHeight="220">
                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                      Padding="10">
                                <TextBlock x:Name="TermsTextBlock"
                                           FontSize="12"
                                           Foreground="{StaticResource BodyBrush}"
                                           TextWrapping="Wrap"
                                           LineHeight="18"/>
                        </ScrollViewer>
                </Border>

                <CheckBox x:Name="AcceptTermsCheckBox"
                          Grid.Row="4"
                          Margin="0,0,0,14"
                          Foreground="{StaticResource TextBrush}"
                          Content="I have read and accept the Beta Terms of Use, including automatic usage reporting."
                          Checked="AcceptTermsCheckBox_Changed"
                          Unchecked="AcceptTermsCheckBox_Changed"/>

                <TextBlock Grid.Row="5"
                           Text="Name"
                           Foreground="{StaticResource TextBrush}"
                           Margin="0,0,0,4"/>

                <TextBox x:Name="NameTextBox"
                         Grid.Row="6"
                         Margin="0,0,0,12"
                         Padding="6,4"
                         FontSize="13"/>

                <TextBlock Grid.Row="7"
                           Text="Email"
                           Foreground="{StaticResource TextBrush}"
                           Margin="0,0,0,4"/>

                <TextBox x:Name="EmailTextBox"
                         Grid.Row="8"
                         Margin="0,0,0,8"
                         Padding="6,4"
                         FontSize="13"/>

                <TextBlock x:Name="ValidationTextBlock"
                           Grid.Row="9"
                           Foreground="Red"
                           FontSize="12"
                           TextWrapping="Wrap"
                           Visibility="Collapsed"
                           Margin="0,0,0,12"/>

                <StackPanel Grid.Row="10"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                        <Button x:Name="ContinueButton"
                                Content="Continue"
                                Width="100"
                                IsDefault="True"
                                IsEnabled="False"
                                Click="Continue_Click"/>
                </StackPanel>
        </Grid>
</Window>
