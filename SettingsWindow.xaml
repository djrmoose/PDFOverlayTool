<Window x:Class="PdfOverlayTool.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Icon="/Resources/OCT.ico"
        Width="480"
        Height="Auto"
        SizeToContent="Height"
        MinWidth="420"
        WindowStartupLocation="CenterOwner"
        Background="#F3EDE4">

        <Window.Resources>
                <SolidColorBrush x:Key="AccentBrush" Color="#7A5230"/>
                <SolidColorBrush x:Key="TextBrush" Color="#3A2E24"/>
                <SolidColorBrush x:Key="HelpTextBrush" Color="#4A3F35"/>
                <SolidColorBrush x:Key="PanelBorderBrush" Color="#C4B5A3"/>
                <SolidColorBrush x:Key="ControlBackgroundBrush" Color="#FDF9F4"/>
                <Style TargetType="Button">
                        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
                        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                        <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Padding" Value="12,6"/>
                        <Setter Property="FontSize" Value="13"/>
                </Style>
        </Window.Resources>

        <Grid Margin="24">
                <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Settings"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="{StaticResource TextBrush}"
                           Margin="0,0,0,12"/>

                <TextBlock Grid.Row="1"
                           Text="Display"
                           FontSize="15"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource AccentBrush}"
                           Margin="0,0,0,8"/>

                <CheckBox x:Name="ColorBlindFriendlyCheckBox"
                          Grid.Row="2"
                          Margin="0,0,0,8"
                          Foreground="{StaticResource TextBrush}"
                          Content="Color-blind friendly colors (blue / orange comparison tints)"
                          Checked="ColorBlindFriendlyCheckBox_Changed"
                          Unchecked="ColorBlindFriendlyCheckBox_Changed"/>

                <TextBlock Grid.Row="3"
                           FontSize="13"
                           Foreground="{StaticResource HelpTextBrush}"
                           TextWrapping="Wrap"
                           LineHeight="20"
                           Margin="0,0,0,20"
                           Text="Uses blue for the base layer and orange for the overlay instead of green and red. File name labels and AUTO mode indicators follow the same palette."/>

                <StackPanel Grid.Row="4"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                        <Button x:Name="ResetDefaultsButton"
                                Content="Reset All Settings to Default"
                                Margin="0,0,12,0"
                                Click="ResetDefaultsButton_Click"/>
                        <Button Content="Close"
                                Width="90"
                                IsCancel="True"
                                IsDefault="True"/>
                </StackPanel>
        </Grid>
</Window>
